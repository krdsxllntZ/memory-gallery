# 🖼️ คู่มือลดขนาดไฟล์รูปภาพ

คู่มือนี้จะสอนวิธีลดขนาดไฟล์รูปภาพเพื่อให้เว็บโหลดเร็วขึ้น

---

## 🎯 เป้าหมาย

- ลดขนาดไฟล์รูปภาพ 50-80% โดยไม่เสียคุณภาพมาก
- ทำให้เว็บโหลดเร็วขึ้น โดยเฉพาะบนมือถือ
- ประหยัด bandwidth และ storage

---

## 🛠️ วิธีที่ 1: ใช้สคริปต์ Python (แนะนำ)

### ขั้นตอน

1. **ติดตั้ง Pillow** (library สำหรับจัดการรูปภาพ)
   ```bash
   pip install Pillow
   ```

2. **รันสคริปต์**
   ```bash
   cd d:\M6\memory-gallery
   python scripts\optimize_images.py
   ```

3. **รอสักครู่** สคริปต์จะ:
   - สร้าง backup รูปภาพเดิมไว้ที่ `public/images_backup/`
   - ลดขนาดรูปภาพทั้งหมด (resize + compress)
   - แสดงสรุปผลการลดขนาด

4. **ตรวจสอบผลลัพธ์**
   - รูปภาพจะถูก optimize แล้ว (เขียนทับไฟล์เดิม)
   - ถ้าต้องการคืนค่า ให้ copy จาก `public/images_backup/` กลับมา

---

## 🛠️ วิธีที่ 2: ใช้เว็บไซต์ออนไลน์ (ง่าย แต่ทำทีละไฟล์)

### เว็บไซต์ที่แนะนำ

1. **TinyPNG** - [tinypng.com](https://tinypng.com)
   - ลากไฟล์วางได้ทีละหลายไฟล์
   - ลดขนาดได้ดีมาก (PNG/JPG)

2. **Squoosh** - [squoosh.app](https://squoosh.app)
   - ควบคุมคุณภาพได้ละเอียด
   - รองรับหลาย format (WebP, AVIF)

3. **ImageOptim** - [imageoptim.com](https://imageoptim.com)
   - มี desktop app สำหรับ Mac

### ขั้นตอน

1. เข้าเว็บไซต์
2. ลากไฟล์รูปภาพวาง (หรือเลือกไฟล์)
3. รอให้ process เสร็จ
4. ดาวน์โหลดไฟล์ที่ optimize แล้ว
5. แทนที่ไฟล์เดิมใน `public/images/`

---

## 🛠️ วิธีที่ 3: ใช้โปรแกรม Desktop

### Windows

- **ImageOptimizer** - [github.com/fawazahmed0/image-optimizer](https://github.com/fawazahmed0/image-optimizer)
- **Caesium** - [caesium.app](https://caesium.app)

### Mac

- **ImageOptim** - [imageoptim.com/mac](https://imageoptim.com/mac)
- **Squoosh Desktop** - [squoosh.app](https://squoosh.app)

---

## 📊 การตั้งค่าแนะนำ

### สำหรับ Mobile-First Website

- **ความกว้างสูงสุด**: 1920px (Full HD)
- **คุณภาพ JPEG**: 80-85%
- **คุณภาพ WebP**: 80-85%
- **PNG Compression**: Level 6-8

### สำหรับรูปภาพใน Gallery

- **Thumbnail**: 400x400px (สำหรับ grid)
- **Full Image**: 1920px width (สำหรับ modal)

---

## 💡 เทคนิคเพิ่มเติม

### 1. ใช้ WebP Format

WebP ให้คุณภาพดีกว่า JPEG/PNG แต่ไฟล์เล็กกว่า 30-50%

**แปลงเป็น WebP:**
```bash
# ใช้สคริปต์ Python (รองรับ WebP แล้ว)
# หรือใช้ Squoosh.app
```

### 2. Lazy Loading

เว็บนี้มี lazy loading อยู่แล้ว (`loading="lazy"` ใน `<img>`) แต่ถ้าต้องการเพิ่ม:

```tsx
<img src={img.src} loading="lazy" ... />
```

### 3. Responsive Images

ใช้ `srcset` สำหรับแสดงรูปขนาดต่างกันตามหน้าจอ:

```tsx
<img
  src={img.src}
  srcSet={`${img.src}?w=400 400w, ${img.src}?w=800 800w`}
  sizes="(max-width: 640px) 400px, 800px"
/>
```

---

## ⚠️ ข้อควรระวัง

1. **Backup ก่อน optimize** - สคริปต์จะ backup ให้อัตโนมัติ แต่ควร backup เองด้วย
2. **ทดสอบหลัง optimize** - ตรวจสอบว่ารูปภาพยังดูดีอยู่
3. **อย่าลดคุณภาพมากเกินไป** - อาจทำให้รูปภาพดูแย่

---

## 📈 ผลลัพธ์ที่คาดหวัง

- **ขนาดไฟล์ลดลง**: 50-80%
- **เวลาโหลดลดลง**: 60-80%
- **คุณภาพภาพ**: ยังดูดี (ไม่ต่างจากเดิมมาก)

---

## 🔄 หลังจาก Optimize แล้ว

1. **ทดสอบเว็บ** - ตรวจสอบว่ารูปภาพแสดงผลถูกต้อง
2. **Push ขึ้น GitHub** - ดูคู่มือใน `GITHUB_GUIDE.md`
3. **Deploy ใหม่บน Vercel** - Vercel จะ rebuild อัตโนมัติ

---

## 🐛 แก้ปัญหา

### ถ้า Python script ไม่ทำงาน

```bash
# ตรวจสอบว่า Pillow ติดตั้งแล้วหรือยัง
pip list | findstr Pillow

# ถ้ายังไม่มี
pip install Pillow
```

### ถ้ารูปภาพเสียหายหลัง optimize

- Copy ไฟล์จาก `public/images_backup/` กลับมา
- ลองใช้วิธีอื่น (TinyPNG, Squoosh)

---

## 📚 เรียนรู้เพิ่มเติม

- [Web.dev - Image Optimization](https://web.dev/fast/#optimize-your-images)
- [Google - Image Optimization Guide](https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/image-optimization)
