# 📝 คู่มือแก้ไขข้อความในเว็บไซต์

ไฟล์นี้บอกตำแหน่งที่คุณสามารถแก้ไขข้อความต่างๆ ในเว็บไซต์ได้

## 📍 ตำแหน่งที่แก้ไขได้

### 1. "Photo Archive" (มุมบนซ้าย)
**ไฟล์:** `src/App.tsx`  
**บรรทัด:** ประมาณ 60  
**โค้ด:**
```tsx
<span className="text-xs uppercase tracking-[0.25em] text-ink-500">Photo Archive</span>
```

### 2. ข้อความ Hero Section (หน้าแรก)
**ไฟล์:** `src/App.tsx`  
**ฟังก์ชัน:** `GalleryPage`  
**บรรทัด:** ประมาณ 155-167

#### 2.1 หัวข้อหลัก
```tsx
<h2 className="text-2xl font-semibold leading-snug text-ink-900">
  รวม{" "}
  <span className="text-ink-900 underline decoration-accent-pink/60 decoration-4 underline-offset-4">
    Childhood – ม.ต้น – ม.4 – ม.5 – ม.6
  </span>{" "}
  ไว้ในที่เดียว
</h2>
```

#### 2.2 คำอธิบาย
```tsx
<p className="text-sm leading-relaxed text-ink-700">
  เลื่อนลงเพื่อดูภาพแบบสุ่มจากทุกช่วงเวลาในชีวิตนักเรียน แตะที่รูปเพื่อขยายเต็มจอและดูโค้ดรูปภาพ
</p>
```

#### 2.3 หมายเหตุ
```tsx
<p className="text-[11px] text-ink-500">
  * เว็บไซต์นี้ออกแบบให้ใช้งานง่ายบนมือถือ แต่รองรับจอใหญ่ด้วย
</p>
```

### 3. "Memory Gallery" (ชื่อเว็บ)
**ไฟล์:** `src/App.tsx`  
**บรรทัด:** ประมาณ 61  
**โค้ด:**
```tsx
<h1 className="text-lg font-semibold text-ink-900">Memory Gallery</h1>
```

### 4. ข้อความอื่นๆ

#### 4.1 ปุ่มเมนู
- **"หน้า 1: แกลเลอรี"** - บรรทัด ~73
- **"หน้า 2: ค้นหา"** - บรรทัด ~84
- **"ติดต่อ"** - บรรทัด ~95
- **"บอกรูปที่คุณชอบให้ผมฟังหน่อยสิ"** - บรรทัด ~103

#### 4.2 ข้อความในหน้าค้นหา
- **"ค้นหาจากรหัสรูปภาพ"** - บรรทัด ~219
- **"เลือกช่วงชั้น / หมวดหมู่"** - บรรทัด ~243
- **"พบภาพทั้งหมด X ภาพ"** - บรรทัด ~263

#### 4.3 ข้อความในหน้าติดต่อ
- **"ติดต่อ"** - บรรทัด ~404
- **"สามารถติดต่อได้ผ่านช่องทางต่างๆ ด้านล่าง"** - บรรทัด ~405

---

## 💡 วิธีแก้ไข

1. เปิดไฟล์ `src/App.tsx` ในโปรเจกต์
2. ค้นหาข้อความที่ต้องการแก้ (ใช้ Ctrl+F)
3. แก้ไขข้อความในเครื่องหมาย `"..."` หรือ `{"..."}`
4. บันทึกไฟล์
5. รัน `npm run dev` เพื่อดูผลลัพธ์

---

## 📌 หมายเหตุ

- ข้อความที่อยู่ใน `className` หรือ `id` ไม่ควรแก้ (เป็น CSS classes)
- ข้อความที่อยู่ใน `href`, `src`, `target` เป็นลิงก์/ที่อยู่ไฟล์ ไม่ควรแก้
- แก้เฉพาะข้อความที่แสดงผลบนหน้าจอเท่านั้น
