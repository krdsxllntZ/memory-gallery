# 🚀 วิธี Deploy ขึ้น Vercel

มี 2 วิธีหลักในการ deploy โปรเจกต์นี้ขึ้น Vercel

---

## วิธีที่ 1: ผ่าน Vercel Dashboard (แนะนำ)

### ขั้นตอน

1. **Push โปรเจกต์ขึ้น GitHub**
   - สร้าง repository ใหม่บน [github.com](https://github.com)
   - ในโฟลเดอร์ `memory-gallery` รันคำสั่ง:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ชื่อผู้ใช้/ชื่อrepo.git
   git push -u origin main
   ```

2. **เข้า Vercel**
   - ไปที่ [vercel.com](https://vercel.com) แล้ว Sign in (ใช้ GitHub ได้เลย)

3. **Import โปรเจกต์**
   - กด **Add New...** → **Project**
   - เลือก repository ที่ push ไว้
   - Vercel จะ auto-detect ว่าเป็น Vite project
   - กด **Deploy** ได้เลย (ไม่ต้องเปลี่ยน settings)

4. **รอสักครู่** แล้วจะได้ลิงก์เว็บ เช่น `https://memory-gallery-xxx.vercel.app`

---

## วิธีที่ 2: ผ่าน Vercel CLI

### ขั้นตอน

1. **ติดตั้ง Vercel CLI** (ถ้ายังไม่มี)
   ```bash
   npm i -g vercel
   ```

2. **Deploy**
   - เข้าโฟลเดอร์โปรเจกต์:
   ```bash
   cd memory-gallery
   ```
   - รันคำสั่ง:
   ```bash
   vercel
   ```
   - ถ้าเป็นครั้งแรก จะถามให้ login (กด Y แล้วเปิดลิงก์ใน browser)
   - ตอบคำถาม:
     - **Set up and deploy?** → Y
     - **Which scope?** → เลือก account ของคุณ
     - **Link to existing project?** → N
     - **Project name?** → กด Enter (ใช้ชื่อ memory-gallery)
     - **In which directory is your code?** → กด Enter (ใช้ ./)

3. **รอ build เสร็จ** จะได้ลิงก์เว็บทันที

### Deploy ครั้งถัดไป (อัปเดตเว็บ)
```bash
vercel --prod
```

---

## ⚠️ สิ่งที่ต้องตรวจสอบก่อน Deploy

1. **รูปภาพ** ต้องอยู่ใน `public/images/` เพราะ Vercel จะ copy โฟลเดอร์ `public` ไปด้วย
2. **ขนาด repo** ถ้ารูปเยอะมาก (>100MB) อาจต้องใช้ Git LFS หรือเก็บรูปที่อื่น
3. **Environment** ไม่ต้องตั้ง env variable สำหรับโปรเจกต์นี้ (ไม่มี API key)

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Vercel Docs - Deploy](https://vercel.com/docs/deployments/overview)
- [Vercel + Vite](https://vercel.com/guides/deploying-vite-with-vercel)
